import React, { Component } from "react";

class Home extends Component {
  state = { };

  componentDidMount = async () => {

  };

  render() {
    return (
        <h1>Accueil</h1>
    );
  }
}

export default Home;
